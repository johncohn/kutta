// Copy this file to wifi_secrets.h (gitignored) and fill in your network.
// wifi_secrets.h is never committed, so credentials never end up in git.

#pragma once

const char *WIFI_SSID = "your-network-name";
const char *WIFI_PASSWORD = "your-network-password";
