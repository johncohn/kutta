# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# Build output
/airfoil
/snapshot
/kutta

# Perf/event logs written by -debug, kept around while tuning the exhibit
*.debug

# GitHub Pages build output: the workflow rebuilds both on every deploy, and
# wasm_exec.js has to match the toolchain rather than sit here going stale.
/web/kutta.wasm
/web/wasm_exec.js

.DS_Store

dist/
notes.md
release.sh

*.png
!assets/*.png

TODO.md

# Working notes for issue/PR drafts, not part of the project
/pr-drafts/

# ESP32 knob controller firmware: real WiFi credentials, never committed
deploy/esp32_knob_controller/wifi_secrets.h

